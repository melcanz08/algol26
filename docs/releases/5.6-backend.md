# 5.6 Backend Independence — Spec

Goal: Semantic IR must be backend independent, differential execution must agree.

## Checks
- `test_semantic_ir_is_backend_independent` — IR complete, no backend tags
- `test_semantic_ir_is_deterministic` — build twice same
- `test_56_arithmetic_diff` — interpreter vs ir_codegen same output
- `test_56_string_diff` — String.concat same
- `test_56_list_diff` — lists same
- `test_56_borrow_not_removed` — optimizer does not delete Borrow/AddrOf/Deref

## Command
```bash
cargo test --test backend_independence -- --nocapture
cargo test --test backend_diff -- --nocapture
```

Status: PARTIAL -> DONE after this PR