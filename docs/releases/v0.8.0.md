## Level 5 Hardening — 20/20 corpus, 27/27 tests

This release locks Level 5.1 / 5.2 / 5.3 / 5.4 / 5.5 with zero ICE and full diagnostics.

### 5.1 Negative Corpus (20 files) — no ICE
`tests/integration/negative/` total=20 invalid=20
Enforced via OR logic:
```rust
let is_invalid = !diags.is_empty() || analyzer.analyze().is_err()
```
- Borrow/move E0007 caught by `SemanticAnalyzer`
- Break/defer caught by `SemanticIRBuilder`
- Files: `07_missing_return`, `08_arity_mismatch`, `09_duplicate_decl`, `10_assign_to_val`, `11_break_outside_loop`, `12_defer_in_global`, `13_invalid_return_type`, `14_mut_borrow_of_val`, `15_redecl_function`, `16_invalid_for_type`, `17_deref_non_ref`, `18_invalid_binary_string_int`, `19_use_after_scope`, `20_invalid_nested_if_defer`, `double_borrow`, `use_after_move`, `undefined_var`, `type_mismatch`, `invalid_syntax`, `invalid_array_index`

### 5.3 Stress
- 100 vars single scope, 10-level nested if/for/defer/break/return, 10-level closure capture, nested functions, multiple imports

### 5.4 Optimizer Safety
- Fixed `String.concat` codegen
- `test_optimization_preserves_borrow_semantics`: before=2 after=2
- Interpreter diff preserves semantics for lists/strings/complex
- Idempotent

### 5.5 Diagnostics Quality
- `double_borrow.al26`: ` Cannot mutably borrow 'arr' while immutably borrowed` + help
- `use_after_move.al26`: ` Use of moved variable 's'` + help
- All 20 produce `E000x` with structured help

### 5.2 Docs
- `CHANGELOG.md`, `docs/releases/v0.8.0-hardening.md`, README hardening section

### Verification
```bash
cargo test --test integration_tests -- --nocapture
# 27 passed
ls tests/integration/negative/ | wc -l # 20
```

**Tags:** `v0.8.0-hardening` -> `v0.8.0` (both on main)
