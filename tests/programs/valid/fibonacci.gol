procedure main
    var n := 10.0
    var a := 0.0
    var b := 1.0
    
    var count := 0.0
    while count < n do
        Terminal.print(a)
        var temp := a + b
        a := b
        b := temp
        count := count + 1.0
