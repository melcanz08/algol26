procedure main
    val a := 10
    val b := 3
    val c := 2.5
    
    val sum_int := a + b
    val sum_mixed := a + c
    val diff_mixed := c - a
    val prod_mixed := a * c
    val div_mixed := a / c
    
    print(sum_int)
    print(sum_mixed)
    print(diff_mixed)
    print(prod_mixed)
    print(div_mixed)
    
    if a < c then
        print("a is less than c")
    
    if a > 5.0 then
        print("a is greater than 5")
