// Scope-based move semantics
procedure main
    val x := 10.0
    
    if true then
        val y := x    // Copy (float is Copy type)
        print(y)
    end
    
    print(x)    // Valid - x was copied, not moved